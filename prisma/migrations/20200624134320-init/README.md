# Migration `20200624134320-init`

This migration has been generated by Dennis Glasberg at 6/24/2020, 1:43:20 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `gberg927_disney-metadata`.`Park` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`id` int NOT NULL  AUTO_INCREMENT,`latitude` Decimal(65,30) NOT NULL  ,`longitude` Decimal(65,30) NOT NULL  ,`name` varchar(191) NOT NULL  ,`slug` varchar(191) NOT NULL  ,`timezone` varchar(191) NOT NULL  ,`updatedAt` datetime(3)   ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `gberg927_disney-metadata`.`Ride` (
`area` varchar(191)   ,`category` ENUM('ARCADE', 'CHARACTER_MEET', 'PLAY_AREA', 'RIDE', 'SHOW_CONTINUOUS', 'SHOW_SCHEDULED', 'WALKTHROUGH')   ,`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`duration` Decimal(65,30)   ,`fastPass` boolean   ,`heightRestriction` int   ,`id` int NOT NULL  AUTO_INCREMENT,`latitude` Decimal(65,30)   ,`longId` varchar(191)   ,`longitude` Decimal(65,30)   ,`name` varchar(191) NOT NULL  ,`openedOn` datetime(3)   ,`parkId` int NOT NULL ,`riderSwap` boolean   ,`singleRider` boolean   ,`slug` varchar(191) NOT NULL  ,`type` ENUM('ATTRACTION', 'ENTERTAINMENT')   ,`updatedAt` datetime(3)   ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `gberg927_disney-metadata`.`WaitTime` (
`active` boolean NOT NULL  ,`amount` int NOT NULL  ,`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`id` int NOT NULL  AUTO_INCREMENT,`jobId` int NOT NULL ,`rideId` int  ,`status` varchar(191) NOT NULL  ,`timestamp` datetime(3) NOT NULL  ,`updatedAt` datetime(3)   ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `gberg927_disney-metadata`.`User` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`email` varchar(191) NOT NULL  ,`id` int NOT NULL  AUTO_INCREMENT,`password` varchar(191) NOT NULL  ,`updatedAt` datetime(3)   ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `gberg927_disney-metadata`.`Job` (
`created` int NOT NULL DEFAULT 0 ,`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`endTime` datetime(3)   ,`id` int NOT NULL  AUTO_INCREMENT,`startTime` datetime(3) NOT NULL  ,`updatedAt` datetime(3)   ,`userId` int NOT NULL ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE UNIQUE INDEX `User.email` ON `gberg927_disney-metadata`.`User`(`email`)

ALTER TABLE `gberg927_disney-metadata`.`Ride` ADD FOREIGN KEY (`parkId`) REFERENCES `gberg927_disney-metadata`.`Park`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `gberg927_disney-metadata`.`WaitTime` ADD FOREIGN KEY (`rideId`) REFERENCES `gberg927_disney-metadata`.`Ride`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE `gberg927_disney-metadata`.`WaitTime` ADD FOREIGN KEY (`jobId`) REFERENCES `gberg927_disney-metadata`.`Job`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `gberg927_disney-metadata`.`Job` ADD FOREIGN KEY (`userId`) REFERENCES `gberg927_disney-metadata`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200624134320-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,93 @@
+datasource db {
+  provider = "mysql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Park {
+  id        Int       @id @default(autoincrement())
+  name      String
+  slug      String
+  latitude  Float
+  longitude Float
+  timezone  String
+  createdAt DateTime  @default(now())
+  updatedAt DateTime? @updatedAt
+  rides     Ride[]
+}
+
+enum RideCategory {
+  ARCADE
+  CHARACTER_MEET
+  PLAY_AREA
+  RIDE
+  SHOW_CONTINUOUS
+  SHOW_SCHEDULED
+  WALKTHROUGH
+}
+
+enum RideType {
+  ATTRACTION
+  ENTERTAINMENT
+}
+
+model Ride {
+  id                Int           @id @default(autoincrement())
+  name              String
+  slug              String
+  longId            String?
+  category          RideCategory?
+  type              RideType?
+  longitude         Float?
+  latitude          Float?
+  area              String?
+  openedOn          DateTime?
+  heightRestriction Int?
+  duration          Float?
+  fastPass          Boolean?
+  singleRider       Boolean?
+  riderSwap         Boolean?
+  createdAt         DateTime      @default(now())
+  updatedAt         DateTime?     @updatedAt
+  park              Park          @relation(fields: [parkId], references: [id])
+  parkId            Int
+  waitTimes         WaitTime[]
+}
+
+model WaitTime {
+  id        Int       @id @default(autoincrement())
+  timestamp DateTime
+  amount    Int
+  active    Boolean
+  status    String
+  createdAt DateTime  @default(now())
+  updatedAt DateTime? @updatedAt
+  ride      Ride?     @relation(fields: [rideId], references: [id])
+  rideId    Int?
+  job       Job       @relation(fields: [jobId], references: [id])
+  jobId     Int
+}
+
+model User {
+  id        Int       @id @default(autoincrement())
+  email     String    @unique
+  password  String
+  createdAt DateTime  @default(now())
+  updatedAt DateTime? @updatedAt
+  jobs      Job[]
+}
+
+model Job {
+  id        Int        @id @default(autoincrement())
+  startTime DateTime
+  endTime   DateTime?
+  created   Int        @default(0)
+  createdAt DateTime   @default(now())
+  updatedAt DateTime?  @updatedAt
+  user      User       @relation(fields: [userId], references: [id])
+  userId    Int
+  waitTimes WaitTime[]
+}
```


