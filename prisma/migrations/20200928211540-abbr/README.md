# Migration `20200928211540-abbr`

This migration has been generated by Dennis Glasberg at 9/28/2020, 5:15:40 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX `userId` ON `gberg927_disney-metadata`.`Job`

DROP INDEX `resortId` ON `gberg927_disney-metadata`.`Park`

DROP INDEX `parkId` ON `gberg927_disney-metadata`.`Ride`

DROP INDEX `jobId` ON `gberg927_disney-metadata`.`WaitTime`

DROP INDEX `rideId` ON `gberg927_disney-metadata`.`WaitTime`

ALTER TABLE `gberg927_disney-metadata`.`Park` ADD COLUMN `abbreviation` varchar(191)  NOT NULL DEFAULT 'abbr'

ALTER TABLE `gberg927_disney-metadata`.`Resort` ADD COLUMN `abbreviation` varchar(191)  NOT NULL DEFAULT 'abbr'

ALTER TABLE `gberg927_disney-metadata`.`Job` ADD FOREIGN KEY (`userId`) REFERENCES `gberg927_disney-metadata`.`User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `gberg927_disney-metadata`.`Park` ADD FOREIGN KEY (`resortId`) REFERENCES `gberg927_disney-metadata`.`Resort`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `gberg927_disney-metadata`.`Ride` ADD FOREIGN KEY (`parkId`) REFERENCES `gberg927_disney-metadata`.`Park`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `gberg927_disney-metadata`.`WaitTime` ADD FOREIGN KEY (`rideId`) REFERENCES `gberg927_disney-metadata`.`Ride`(`id`) ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE `gberg927_disney-metadata`.`WaitTime` ADD FOREIGN KEY (`jobId`) REFERENCES `gberg927_disney-metadata`.`Job`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

CREATE UNIQUE INDEX `User.email_unique` ON `gberg927_disney-metadata`.`User`(`email`);
DROP INDEX `User.email` ON `gberg927_disney-metadata`.`User`
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200626171556-init..20200928211540-abbr
--- datamodel.dml
+++ datamodel.dml
@@ -1,32 +1,34 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 model Resort {
-  id    Int    @id @default(autoincrement())
-  name  String
-  slug  String
-  parks Park[]
+  id           Int    @id @default(autoincrement())
+  name         String
+  slug         String
+  abbreviation String @default("abbr")
+  parks        Park[]
 }
 model Park {
-  id        Int       @id @default(autoincrement())
-  name      String
-  slug      String
-  latitude  Float
-  longitude Float
-  timezone  String
-  createdAt DateTime  @default(now())
-  updatedAt DateTime? @updatedAt
-  resort    Resort    @relation(fields: [resortId], references: [id])
-  resortId  Int
-  rides     Ride[]
+  id           Int       @id @default(autoincrement())
+  name         String
+  slug         String
+  abbreviation String    @default("abbr")
+  latitude     Float
+  longitude    Float
+  timezone     String
+  createdAt    DateTime  @default(now())
+  updatedAt    DateTime? @updatedAt
+  resort       Resort    @relation(fields: [resortId], references: [id])
+  resortId     Int
+  rides        Ride[]
 }
 enum RideCategory {
   ARCADE
```


