# Migration `20200618164844-initial`

This migration has been generated by Dennis Glasberg at 6/18/2020, 4:48:44 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "RideCategory" AS ENUM ('ARCADE', 'CHARACTER_MEET', 'PLAY_AREA', 'RIDE', 'SHOW_CONTINUOUS', 'SHOW_SCHEDULED', 'WALKTHROUGH');

CREATE TYPE "RideType" AS ENUM ('ATTRACTION', 'ENTERTAINMENT');

CREATE TABLE "public"."Park" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"latitude" Decimal(65,30)  NOT NULL ,"longitude" Decimal(65,30)  NOT NULL ,"name" text  NOT NULL ,"slug" text  NOT NULL ,"timezone" text  NOT NULL ,"updatedAt" timestamp(3)   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Ride" (
"area" text   ,"category" "RideCategory"  ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"duration" Decimal(65,30)   ,"fastPass" boolean   ,"heightRestriction" integer   ,"id" SERIAL,"latitude" Decimal(65,30)   ,"longId" text   ,"longitude" Decimal(65,30)   ,"name" text  NOT NULL ,"openedOn" timestamp(3)   ,"parkId" integer  NOT NULL ,"riderSwap" boolean   ,"singleRider" boolean   ,"slug" text  NOT NULL ,"type" "RideType"  ,"updatedAt" timestamp(3)   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."WaitTime" (
"active" text  NOT NULL ,"amount" integer  NOT NULL ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"jobId" integer  NOT NULL ,"rideId" integer  NOT NULL ,"status" text  NOT NULL ,"timestamp" timestamp(3)  NOT NULL ,"updatedAt" timestamp(3)   ,"userId" integer   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."User" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text  NOT NULL ,"id" SERIAL,"password" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Job" (
"created" integer  NOT NULL ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"endTime" timestamp(3)  NOT NULL ,"id" SERIAL,"startTime" timestamp(3)  NOT NULL ,"updatedAt" timestamp(3)   ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."Ride" ADD FOREIGN KEY ("parkId")REFERENCES "public"."Park"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."WaitTime" ADD FOREIGN KEY ("rideId")REFERENCES "public"."Ride"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."WaitTime" ADD FOREIGN KEY ("jobId")REFERENCES "public"."Job"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."WaitTime" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Job" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200618164844-initial
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,96 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Park {
+  id        Int       @id @default(autoincrement())
+  name      String
+  slug      String
+  latitude  Float
+  longitude Float
+  timezone  String
+  createdAt DateTime  @default(now())
+  updatedAt DateTime? @updatedAt
+  Ride      Ride[]
+}
+
+enum RideCategory {
+  ARCADE
+  CHARACTER_MEET
+  PLAY_AREA
+  RIDE
+  SHOW_CONTINUOUS
+  SHOW_SCHEDULED
+  WALKTHROUGH
+}
+
+enum RideType {
+  ATTRACTION
+  ENTERTAINMENT
+}
+
+model Ride {
+  id                Int           @id @default(autoincrement())
+  name              String
+  slug              String
+  longId            String?
+  category          RideCategory?
+  type              RideType?
+  longitude         Float?
+  latitude          Float?
+  area              String?
+  openedOn          DateTime?
+  heightRestriction Int?
+  duration          Float?
+  fastPass          Boolean?
+  singleRider       Boolean?
+  riderSwap         Boolean?
+  createdAt         DateTime      @default(now())
+  updatedAt         DateTime?     @updatedAt
+  park              Park          @relation(fields: [parkId], references: [id])
+  parkId            Int
+  WaitTime          WaitTime[]
+}
+
+model WaitTime {
+  id        Int       @id @default(autoincrement())
+  timestamp DateTime
+  amount    Int
+  active    String
+  status    String
+  createdAt DateTime  @default(now())
+  updatedAt DateTime? @updatedAt
+  ride      Ride      @relation(fields: [rideId], references: [id])
+  rideId    Int
+  Job       Job       @relation(fields: [jobId], references: [id])
+  jobId     Int
+  User      User?     @relation(fields: [userId], references: [id])
+  userId    Int?
+}
+
+model User {
+  id        Int        @id @default(autoincrement())
+  email     String     @unique
+  password  String
+  createdAt DateTime   @default(now())
+  updatedAt DateTime   @updatedAt
+  Job       Job[]
+  WaitTime  WaitTime[]
+}
+
+model Job {
+  id        Int        @id @default(autoincrement())
+  startTime DateTime
+  endTime   DateTime
+  created   Int
+  createdAt DateTime   @default(now())
+  updatedAt DateTime?  @updatedAt
+  user      User       @relation(fields: [userId], references: [id])
+  userId    Int
+  WaitTime  WaitTime[]
+}
```


